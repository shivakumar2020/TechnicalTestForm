node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    withSonarQubeEnv('sonarqube') {
      bat "mvn clean verify sonar:sonar -Dsonar.projectKey=TestFormProject -Dsonar.projectName='TestFormProject'"
    }
  }
}